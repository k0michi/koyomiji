export interface KnowledgeItem {
  id: string;
  title: string;
  category: string;
  created: string;
  description: string;
}

export interface LogItem {
  id: string;
  title: string;
  created: string;
  description: string;
}